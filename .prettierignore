node_modules
.next
out
dist
build
# Preserve the fixed-image runner's checksum-bound canonical JSON bytes.
scripts/literature/fixtures/post-migration-schema-security-definition-identity.json
# Same reason: the Device Intelligence status overlay is emitted byte-deterministically by
# `npm run ip-intel:status-overlay`, and its `--check` mode compares committed bytes against a
# fresh generation. Prettier collapses single-element arrays, which would make every commit
# report the artifact as stale.
data/ip-device-intelligence/generated/
# FDA refresh research receipts are SHA-pinned by the generated runtime overlay.
data/ip-device-intelligence/research/status-refresh-2026-09-17.json
# The daily-reference batch is SHA-pinned by its immutable acquisition receipt.
docs/ip-device-intelligence/daily-reference-review/*.json
*.log
.env*
.DS_Store
public/socal-ebus-course/app
public/bronch-navigation-trainer/app




# Preserve imported source formatting and asset metadata.
navigation_module
EBUS-course
