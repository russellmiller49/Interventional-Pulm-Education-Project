export default function ToolsPage() {
  return (
    <section className="container space-y-6 py-24">
      <h1 className="text-4xl font-bold tracking-tight">Tools</h1>
      <p className="max-w-2xl text-muted-foreground">
        The tools directory is being rebuilt in a future milestone. Live search, filtering, and GitHub integrations will
        return once the new data layer is finalized.
      </p>
    </section>
  )
}
