/**
 * One version identifies the complete private v1 curriculum.
 *
 * Changing this value intentionally invalidates pre-v3 private-development
 * progress rather than mixing results from materially different content.
 */
export const BAXTER_CRRT_CONTENT_VERSION = '1.0.0-sme-review.1' as const
