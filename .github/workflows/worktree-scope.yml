name: Worktree scope

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

permissions:
  contents: read

jobs:
  registry-scope:
    name: Registry scope
    runs-on: ubuntu-latest
    steps:
      - name: Scope checks retired
        run: echo "Worktree isolation removed; scope checks retired."
