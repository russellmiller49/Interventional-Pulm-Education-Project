# MCS component-test matrix (M5)

What every Mechanical Circulatory Support component is responsible for, where a learner meets it,
which of its branches are covered, by which test, and — where a branch is not covered — the exact
reason.

The matrix is the acceptance record for M5. Coverage percentages appear at the end as a measurement,
never as the definition: a branch counts as covered when a named behaviour test exercises it, and a
branch that no test names is listed as a residual gap even if some other test happened to execute the
line.

- Package: M5, component and integration test backfill.
- Baseline: 14 suites / 378 tests. Final: 21 suites / 744 tests.
- Production diff: `McsWorkbench.tsx`, `McsLearnPrimaryPane.tsx` and `components/teaching/selectors.ts`, and only for the four cross-surface corrections in §4.

## 1. Suites

| Suite                                           | What it holds                                                                                                            |
| ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| `__tests__/m5-workbench-routing.test.tsx`       | Route and activity resolution, fallbacks, precedence, resume banner, selection records, device and section transitions   |
| `__tests__/m5-learn-integration.test.tsx`       | The six-phase Learn runtime, table-driven over all nine authored sections; phase navigation; sequence-derived completion |
| `__tests__/m5-practice-challenge.test.tsx`      | Mechanism Studio, the nine patient cases, the three capstones                                                            |
| `__tests__/m5-controls-and-surfaces.test.tsx`   | Every bounded control, the synchronized monitor and anatomy, the mobile surface semantics and phase focus                |
| `__tests__/m5-persistence-analytics.test.tsx`   | Progress load/write/reset/save-and-exit, exact-once analytics, the privacy payload, the interval and unmount cleanup     |
| `__tests__/m5-module-surfaces.test.tsx`         | Hub, route orientation, module frame and nav, learn landing, sources panel, the reference and evidence drawers           |
| `__tests__/m5-context-bar-consistency.test.tsx` | The cross-surface audit of the patient-context bar (§4)                                                                  |
| `test-support/mcsWorkbenchStubs.tsx`            | The four module boundaries jsdom cannot supply                                                                           |
| `test-support/mcsWorkbench.tsx`                 | Rendering, driving, analytics capture, progress-write counting, timer and animation-frame control                        |

The M0/M1, M2/M3, M4 and content suites are unchanged. Where they already own a contract, M5 cites
them rather than restating them.

## 2. Components

### 2.1 `McsWorkbench.tsx` — 1,024 lines

Route resolution, activity selection, the simulation clock, progress, analytics, and the shell every
other surface hangs off. Appears on `/learn`, `/practice` and `/assess`.

Handlers and effects, one row each.

| Handler / effect                             | Behaviour tested                                                                                                                                                                                 | Test file                                                             | Test name                                                                                                                                                                                                                                                                                                 | Defect found    | Result                  |
| -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- | ----------------------- |
| Initial route + device + activity resolution | First section without a deep link; every lesson id; every device hint; every practice case; every capstone; three invalid-id fallbacks; activity-id-over-device precedence on Learn and Practice | m5-workbench-routing                                                  | `opens the recommended first section…`, `opens the exact section for the %s deep link`, `opens the first authored %s section…`, `opens practice case %s…`, `opens capstone %s…`, `falls back to…` ×3, `lets an exact … id win over a disagreeing device hint` ×2                                          | no              | pass                    |
| Deferred progress load (`setTimeout 0`)      | Not applied during first render; applied after; malformed payload falls back and is left untouched; prior lesson, mastered case and capstone record reflected; unmount before the read is safe   | m5-persistence-analytics                                              | `loads stored progress asynchronously…`, `falls back to a default record…`, `reflects a lesson recorded before this package`, `reflects a mastered case and a stored capstone record…`, `unmounts before the deferred read…`                                                                              | no              | pass                    |
| Selection record write                       | Learn and Practice write the accepted record; Challenge writes none from a deep link; the card write resolves to a real challenge activity; invalid ids write nothing                            | m5-workbench-routing, m5-practice-challenge                           | `writes the Learn selection record…`, `writes the Practice selection record…`, `creates no resume target from a deep-linked Challenge`, `resolves the resume target it writes to a real challenge activity`                                                                                               | no              | pass                    |
| Simulation interval                          | One interval installed; 100 ms normal; 250 ms reduced motion; installed even with no `matchMedia`; cleared on unmount; no state update after unmount                                             | m5-persistence-analytics                                              | `installs exactly one interval, at the normal cadence`, `installs the reduced-motion cadence…`, `still installs the simulation interval where matchMedia does not exist`, `clears the interval it installed on unmount`, `produces no state update when the timers run after unmount`                     | no              | pass                    |
| Aggregate module analytics                   | Payload is exactly device track, station, coarse completion; completion flips to `complete`; no physiologic value in any payload                                                                 | m5-persistence-analytics                                              | `sends only the device track, the station, and a coarse completion state`, `reports completion coarsely once the case is complete`, `sends no physiologic value anywhere in any payload`                                                                                                                  | no              | pass                    |
| Scenario-result persistence                  | Written once after completion; not rewritten on later ticks or rerenders; studio writes nothing                                                                                                  | m5-persistence-analytics, m5-practice-challenge                       | `persists the case result once, and not again on later rerenders`, `records no case result from the studio…`, `records the case result exactly once…`                                                                                                                                                     | no              | pass                    |
| Prediction analytics                         | One event per committed prediction; not repeated while committed; a new case emits its own                                                                                                       | m5-persistence-analytics                                              | `reports a committed prediction once…`, `reports a new prediction for the next case`                                                                                                                                                                                                                      | no              | pass                    |
| Safety-event analytics                       | One event per unique error however long the case runs; a distinct error in another case emits separately; a reset-and-recreate emits again                                                       | m5-persistence-analytics                                              | `reports one safety event for one safety error…`, `reports a distinct safety error in another case separately`, `reports the safety event again when the case is reset…`                                                                                                                                  | no              | pass                    |
| Goal-met analytics                           | None from the studio; none while required actions are outstanding; once when the last lands                                                                                                      | m5-persistence-analytics                                              | `reports no goal met from the studio…`, `reports no goal met while a case still has required actions outstanding`, `reports goal met once when the last required action lands`                                                                                                                            | no              | pass                    |
| Debrief / completion analytics               | Debrief and completion once each; no transfer completion from a case debrief                                                                                                                     | m5-persistence-analytics, m5-practice-challenge                       | `reports the debrief and the completion once each…`, `reveals the deferred response in the debrief, and reports completion once`                                                                                                                                                                          | no              | pass                    |
| `openStudio`                                 | Studio replaces a case; its own activity id per device; bounded controls open; no invented scenario id                                                                                           | m5-practice-challenge                                                 | `is replaced by a case opened from the rail, and restored…`, `reports the studio under its documented activity id and practice mode`, `exposes the bounded %s controls…`                                                                                                                                  | no              | pass                    |
| `selectDevice` (practice)                    | Opens the studio for the chosen device and drops the prior case                                                                                                                                  | m5-workbench-routing                                                  | `opens Mechanism Studio for %s when the device tab changes in Practice`, `drops the previous patient case when the Practice device changes`                                                                                                                                                               | no              | pass                    |
| `selectDevice` (assess)                      | Loads the matching capstone and clears the feedback opt-in                                                                                                                                       | m5-workbench-routing, m5-practice-challenge                           | `loads the %s capstone when the Challenge device tab changes`, `drops the previous opt-in when the learner switches capstone`                                                                                                                                                                             | no              | pass                    |
| `selectDevice` (learn branch)                | —                                                                                                                                                                                                | —                                                                     | —                                                                                                                                                                                                                                                                                                         | —               | **unreachable**, see §5 |
| `chooseLesson`                               | Opens the section, resets the six-phase runtime and the learner's answers, applies the new starting device and starting actions, writes the selection record, preserves unrelated progress       | m5-learn-integration                                                  | `resets the six-phase runtime and the learner answers…`, `gives the new section its own authored starting device and starting actions`, `keeps unrelated stored progress when the section changes`, `offers Back and Ahead…`, `continues from a worked-through section…`                                  | no              | pass                    |
| `recordSectionWorkedThrough`                 | Written only at the transfer endpoint, once, never rewritten on a tick, never duplicated on revisit, preserving case and capstone history and unknown historical ids                             | m5-learn-integration, m5-persistence-analytics                        | `records %s only at the transfer endpoint, and once` (×9), `writes the completion once and does not rewrite it…`, `does not duplicate the id when an already recorded section is revisited`, `preserves the first section, and existing case history…`, `discards no historical id it does not recognize` | no              | pass                    |
| `choosePractice`                             | Opens a case or the studio, updates the rail's current entry and the resume target, drops stale answers, actions and safety events                                                               | m5-practice-challenge                                                 | `opens practice case %s…`, `carries no stale answer, action, or safety event into the next case`                                                                                                                                                                                                          | no              | pass                    |
| `saveAndExit`                                | Writes the current record once and routes to the module front door                                                                                                                               | m5-persistence-analytics                                              | `writes the current record and routes to the module front door on Save & exit`                                                                                                                                                                                                                            | no              | pass                    |
| `resetActivity`                              | Returns the case to its authored start and the Learn section to Recognize, keeps stored history, clears the challenge opt-in                                                                     | m5-practice-challenge, m5-persistence-analytics, m5-learn-integration | `returns the case to its authored start on Reset…`, `keeps stored history when the activity is reset`, `returns the section to Recognize when the activity is reset`                                                                                                                                      | no              | pass                    |
| `selectActivityPhase` (learn)                | Refuses a jump past the furthest phase; allows a return; keeps the furthest phase and the committed answer; moves focus to the activity viewport; closes help                                    | m5-learn-integration                                                  | `refuses a shared-stepper jump past the furthest phase worked through`, `allows a return to an earlier phase…`, `keeps the committed prediction…`, `moves focus back to the activity viewport after a stepper jump`, `closes an open help note when the phase changes`                                    | no              | pass                    |
| `selectActivityPhase` (case)                 | Chooses the mobile surface and focuses the intended region for each phase; navigates without moving the reducer's phase; survives the studio where the regions do not exist                      | m5-controls-and-surfaces                                              | `selects the %s phase, shows the %s surface, and focuses %s` (×5), `selects the monitor and the inspect step when Recognize is chosen`, `navigates with the shared stepper in a case without moving the case phase`, `survives a phase selection in the studio…`                                          | no              | pass                    |
| `showHelp`                                   | Emits the hint event in Practice and not in Challenge; closes on phase and section change                                                                                                        | m5-practice-challenge, m5-learn-integration                           | `emits no hint event when Help is used inside a Challenge`, `emits the hint event when Help is used inside a Practice case`, `closes an open help note when the phase changes`                                                                                                                            | no              | pass                    |
| Patient-context bar                          | §4 in full                                                                                                                                                                                       | m5-context-bar-consistency                                            | 21 tests                                                                                                                                                                                                                                                                                                  | **yes — three** | fixed                   |
| Reference / evidence drawers                 | Open on all three routes, name the active activity, dedupe ids, add transfer evidence, tolerate an empty optional list, leave phase and simulation untouched                                     | m5-module-surfaces                                                    | 9 tests                                                                                                                                                                                                                                                                                                   | no              | pass                    |
| Resume banner                                | Shown on a deep link with the "prior controls and answers were not replayed" sentence; absent without one; named for the lesson or the case                                                      | m5-workbench-routing                                                  | `shows a resume banner on a deep link…`, `names the Learn resume banner…`, `shows no resume banner…`                                                                                                                                                                                                      | no              | pass                    |
| Next-recommended links                       | Exclude the active and the already completed section; exclude the active and mastered case                                                                                                       | m5-learn-integration, m5-practice-challenge                           | `recommends a next section that is neither the active one nor an already recorded one`, `recommends a next case…`                                                                                                                                                                                         | no              | pass                    |
| Launch gate escape hatch                     | On a constrained display the 3D surface offers Save for later (routes to the front door) and Continue on this device                                                                             | m5-controls-and-surfaces                                              | `offers a way out of the 3D surface on a display too small for it`, `offers the same way out of the Learn anatomy pane`                                                                                                                                                                                   | no              | pass                    |

### 2.2 Learn family

| Component                | Responsibility                                                              | Route                    | Important branches                                                                                                                            | Existing coverage                              | New M5 coverage                                                                                                                                                                                                                                       | Kind       | Residual                                                                                    |
| ------------------------ | --------------------------------------------------------------------------- | ------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------- |
| `McsLearnSection`        | Owns the learner's answers and the completion sequence; keyed by section id | learn                    | Six phases; baseline capture on entering Act; transfer setup dispatch; completion effect                                                      | M2/M3 six-phase order; M4 reveal stage         | Complete walk × 9 sections; completion asserted after every phase; reveal stage per phase; answers dropped on section change                                                                                                                          | integrated | `if (index < 0) return` in `advance` — unreachable, the phase always exists                 |
| `McsLearnActionPane`     | Every control the learner touches, per phase                                | learn                    | recognize / predict / act / observe / explain / transfer; inspect-only vs target-control; guided buttons vs workspace controls; verdict; help | M2/M3 focus-after-commit, instruction present  | Precommit DOM empty of the answer; commit shows the verdict without advancing; Continue gated on the resulting state; wrong recognition shows what the pathway shows and still allows Continue; transfer requires both a response and the paired work | integrated | none                                                                                        |
| `McsLearnPrimaryPane`    | The one live surface a section leads with                                   | learn                    | anatomy vs monitor; launch gate                                                                                                               | M2/M3 authored target rendered and highlighted | Surface and target per section; the gate's Save for later                                                                                                                                                                                             | integrated | none — 100%                                                                                 |
| `McsLearnTeachingPane`   | The teaching shell around the M4 panel                                      | learn                    | reveal stages; foundation material                                                                                                            | M4 disclosure staging                          | Reveal stage tracked through all six phases per section; causal ladder at Explain                                                                                                                                                                     | integrated | one branch: the `foundationMaterial` absent arm on a foundation section, which cannot occur |
| `McsCommonModel`         | The shared four-level model                                                 | learn (foundations), hub | `front-door` vs section variant                                                                                                               | M0/M1 and `common-model.test.ts`               | Pane placement unchanged                                                                                                                                                                                                                              | integrated | front-door-only lines, owned by the M0/M1 suite                                             |
| `McsSupportPathwayCards` | The eight standardized pathway cards                                        | learn (mechanisms), hub  | —                                                                                                                                             | M0/M1 field-by-field                           | Rendered only on the mechanisms section                                                                                                                                                                                                               | integrated | one presentation line, owned by M0/M1                                                       |

### 2.3 Simulation surfaces

| Component                                         | Responsibility                                           | Route                         | Important branches                                                                                                                                       | Existing coverage                   | New M5 coverage                                                                                                                                                      | Kind       | Residual                                                                                                                                                                                                      |
| ------------------------------------------------- | -------------------------------------------------------- | ----------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `McsControls`                                     | Every bounded patient and device control                 | practice, assess, learn (act) | 8 patient sliders, rhythm, tamponade; IABP ×5; Impella configuration + 2 pump groups × 4; LVAD ×5; permission gating; Learn highlight                    | one Impella configuration test      | Every control, its bounds, its unit, its dispatch, and its disabled case from a scenario that forbids it                                                             | integrated | none — 100%                                                                                                                                                                                                   |
| `McsCaseWorkflow`                                 | Case identity, the reasoning sequence, coaching, debrief | practice, assess              | studio vs case; inspect ×3; prediction; practice cue vs challenge opt-in; response vs deferred; escalate / reassess / complete; critical errors; debrief | prediction commit, challenge opt-in | Identity for all nine cases; permitted inspections per case; the reasoning rail; escalation; the card's own Reset; replay; deferred response revealed in the debrief | integrated | none — 100%                                                                                                                                                                                                   |
| `McsMonitor`                                      | The synchronized bedside monitor                         | all                           | per-device tiles; alarm bar; four traces; PV loop; trend; causal callout                                                                                 | traces and metric labels            | Device-specific tiles swapping; no non-finite value; alarm priority as text; callout withheld and restored                                                           | integrated | `metric()`'s non-finite arm and two optional tiles — the engine produces no non-finite value through any reachable control, so the em-dash arm is unreachable from the interface                              |
| `McsAnatomyPathwaySummary`                        | The WebGL-free semantic pathway                          | all                           | per-device rows; highlight                                                                                                                               | M2/M3 anatomy targets               | Summary changes with topology; reachable behind the stubbed canvas                                                                                                   | integrated | one highlight arm                                                                                                                                                                                             |
| `McsAnatomy3D`                                    | The 3D canvas                                            | all                           | WebGL support; fallback                                                                                                                                  | —                                   | Mount boundary only, via the stub                                                                                                                                    | boundary   | **0% by design.** jsdom reports no WebGL, and the M5 brief excludes pixel and Three.js validation. Its semantic surface is covered through `McsAnatomyPathwaySummary`, which the stub deliberately keeps real |
| `components/three/*`                              | Three.js meshes                                          | all                           | —                                                                                                                                                        | geometry helpers under test         | none                                                                                                                                                                 | —          | **not rendered in jsdom by design**; `impellaFlowGeometry` and `impellaPlacement` keep their own unit suites                                                                                                  |
| `EcmoCannulationPreview`, `ImpellaVariantPreview` | Lazy comparison visuals on the hub                       | hub                           | —                                                                                                                                                        | —                                   | Fallback boundary asserted through the stub                                                                                                                          | boundary   | interior uncovered by design — same reason as `McsAnatomy3D`                                                                                                                                                  |

### 2.4 Module chrome

| Component               | Responsibility                         | Route    | New M5 coverage                                                                                                                                                                                                                               | Kind       | Residual                                                                                                                                 |
| ----------------------- | -------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| `McsHub`                | The front door                         | `/`      | Counts derived from arrays; studio not counted as a case; recommended section resolves; three open tracks; comparison pathways not claimed as simulated; preview warning in front and gate list behind the disclosure; lazy previews; sources | direct     | one branch, owned by the M0/M1 suite                                                                                                     |
| `McsRouteOrientation`   | What changes between the three routes  | `/`      | Counts via the hub                                                                                                                                                                                                                            | integrated | none — 100%                                                                                                                              |
| `McsModuleFrame`        | Shell, safety notice, English fallback | all      | Safety notice on every route; fallback only on a non-English route                                                                                                                                                                            | integrated | none — 100%                                                                                                                              |
| `McsModuleNav`          | The four module links                  | all      | Active route marked exactly once; four plain links, no gate                                                                                                                                                                                   | integrated | the standalone `McsModuleNav` component export has no consumer — the frame uses `mcsModuleNavItems`. Covered as data, not as a component |
| `McsLearnLanding`       | The pathway landing                    | `/learn` | One link per authored section, in order                                                                                                                                                                                                       | direct     | none — 100%                                                                                                                              |
| `McsSourcesPanel`       | The source list                        | `/`, all | Entries render                                                                                                                                                                                                                                | integrated | one disclosure arm                                                                                                                       |
| `components/teaching/*` | The nine M4 panels                     | learn    | Reveal stage per phase per section, and the congestion provenance staying out of the shared drawer                                                                                                                                            | integrated | M4 owns the exhaustive panel contract; M5 adds only the workbench integration, as the brief requires                                     |

## 3. Action modes

| Mode                  | Sections                                                                                                                                          | Driven through                                                                                                                                                                                                                   |
| --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `inspect-only`        | `mcs-foundations-signals`                                                                                                                         | three guided reading buttons                                                                                                                                                                                                     |
| `sequence`            | `mcs-foundations-mechanisms`                                                                                                                      | the three mechanism-selection buttons, in turn                                                                                                                                                                                   |
| `adjust`              | `iabp-timing-triggering`, `iabp-efficacy-limits`, `impella-unloading-placement`, `lvad-parameters-assessment`, `mcs-device-selection-integration` | the highlighted slider or select, driven until the authored predicate holds                                                                                                                                                      |
| `select`              | `impella-suction-purge-rv`, `lvad-alarms-emergencies`                                                                                             | the right-sided support select; the high-power pattern checkbox                                                                                                                                                                  |
| `identify`, `compare` | none                                                                                                                                              | **declared in `McsLearnActionMode` and used by no authored section.** Nothing renders them, so there is nothing to test; a section that adopts one will fail the `presents the authored %s action mode` table until it is driven |

Two behaviours are pinned so the tables cannot become data replays:

- `reads the adjustment predicate from the resulting state, not from the action id` — moving the
  highlighted control the wrong way dispatches the authored action id and leaves the section
  unsatisfied.
- `does not accept an unrelated control as the requested action` — a different slider moves the
  simulation and does not open Continue.

## 4. Cross-surface consistency audit

Three claims survived in the patient-context bar that M4 had already removed from the panels below
it. Each was demonstrated by a failing test before the correction; fifteen of the eighteen tests in
`m5-context-bar-consistency.test.tsx` failed against the previous workbench.

| #   | Original behaviour                                                                                                                                                                             | Corrected behaviour                                                                                                                                                                                                                                                                                         | Accepted contract                                        |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------- |
| 1   | `shockPhenotype(state)` labelled the patient LV-dominant, RV-dominant or biventricular **shock** from private cut points (contractility 0.55, wedge 20, RAP 14, the simulator's PAPi boundary) | `Filling-pressure congestion pattern`, read through `mcsCongestionProfileId` / `mcsCongestionProfileDefinition` at the ACC-described threshold; the modeled pericardial constraint named separately rather than replacing the pattern; `MCS_CONGESTION_PATTERN_BOUNDARY.doesNotEstablish` carried beside it | M4 congestion framework (`content/congestionProfile.ts`) |
| 2   | `Native / device / effective flow` printed `0.0` for counterpulsation                                                                                                                          | The device slot comes from `flowAccountView`, the same selector the teaching panels use, so counterpulsation reads `none reported`                                                                                                                                                                          | M0–M4: counterpulsation reports no device flow           |
| 3   | `Perfusion` labelled SvO₂ and CPO                                                                                                                                                              | `Modeled balance and pressure–flow summary`, with `mcsDerivedValueGuides.cardiacPowerOutputW.doNotInfer` beside it                                                                                                                                                                                          | The accepted causal ladder and the CPO guide             |

A source lint over the whole component tree keeps the classifier from returning anywhere in it, not
only in the bar: `no MCS component still ships the "%s" label` (×5) and
`classifies congestion only through the accepted framework helper`.

Correction 4 was found in the browser walkthrough rather than in the coverage map — the two surfaces
were both fully covered, and both were internally consistent; only seeing them on one screen showed
that they disagreed. It is the reason the walkthrough is part of the package rather than a formality.

Observed and **not** corrected, because the M5 brief scopes the authorized corrections to the context
bar: `McsMonitor`'s `DEVICE FLOW` tile renders `0.0` for counterpulsation. That tile is accepted M0–M4
behaviour with its own tests, and changing it is a display decision for a later package rather than
one M5 may take.

## 4a. Observed outside M5's scope: the aggregate analytics payload is rejected by the API route

Noticed while reading the network log during the browser walkthrough, and reported rather than
fixed. Every `POST /api/analytics` from the MCS workbench returns **400 Invalid focused
critical-care analytics payload**.

The cause is a contract mismatch that predates M5. `src/app/api/analytics/route.ts` validates MCS
events with a `.strict()` schema requiring four fields:

    { deviceTrack, station, completion, scoreBand }

The workbench sends three — device track, station, coarse completion — which is exactly what the
accepted M0–M4 privacy contract allows and what the module's own privacy note promises. So the
aggregate events are being dropped server-side.

M5 does not correct it, and could not have: the route is a global API file this package must not
touch, and adding `scoreBand` to the payload would add a performance signal to a payload the accepted
contract and this package's own privacy tests hold to three fields. Either side of the mismatch is
someone else's decision. Flagged here so it is a decision rather than an accident.

## 5. Residual uncovered branches in `McsWorkbench`

`McsWorkbench.tsx` finishes at 95.8% statements, 93.0% branches, 98.7% functions, 98.1% lines. Every
remaining gap, with its reason:

| Location                                                                                                           | Branch                                                                                                | Reason it is not covered                                                                                                                                                                                                                                            |
| ------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `selectDevice`, the Learn arm                                                                                      | `const deviceLesson = …; if (deviceLesson) chooseLesson(…)` and the `OPEN_STUDIO` fallback beneath it | **Unreachable through the interface.** Learn replaces the device tabs with the ordered pathway rail, and `selectDevice` is called only from those tabs. Pinned by `replaces the Practice and Challenge device tabs with the ordered pathway rail in Learn`          |
| `if (recordedSafetyEvents.current.has(error)) continue`                                                            | the already-recorded arm                                                                              | No authored case permits two distinct critical errors, so the array cannot grow while already containing one. The _behaviour_ it exists for — one event per unique error — is proved by `reports one safety event for one safety error, however long the case runs` |
| `if (!capstone) return` (×2)                                                                                       | the missing-capstone arm                                                                              | Every device has a capstone; `mcsCapstoneScenarios` is asserted at three per device by the content suite                                                                                                                                                            |
| `if (!next) return` in `chooseLesson`, `if (!scenario) return` in `choosePractice`                                 | the unknown-id arms                                                                                   | The rails offer only real ids; the deep-link equivalents are covered by the three fallback tests                                                                                                                                                                    |
| `if (current.completedLessonIds.includes(lesson.id)) return current`                                               | the already-recorded arm                                                                              | The section effect does not call the handler once the section is complete, so the second guard is defence in depth. `does not duplicate the id when an already recorded section is revisited` proves the outcome                                                    |
| `?? mcsLessons[0]`, `?? 'CAP-IABP-01'`, `?? lesson.summary`, `?? 'MCS challenge'`, `?? 'none reported'`, `?? null` | fallback arms                                                                                         | Defensive defaults for data states the authored content cannot produce; the content suite asserts every lesson has a contract, a title and a summary                                                                                                                |
| `state.causalExplanation \|\| state.scenario.debrief.join(' ')`                                                    | the empty-explanation arm                                                                             | The engine always produces a causal explanation at completion                                                                                                                                                                                                       |

No route, completion, persistence, analytics, feedback, reset, device-switch or focus branch is
uncovered.

## 6. Coverage

`npx jest src/features/mechanical-circulatory-support --coverage --collectCoverageFrom='src/features/mechanical-circulatory-support/components/**/*.{ts,tsx}'`

| Scope                          | Baseline stmts / branch / funcs / lines | Final stmts / branch / funcs / lines        |
| ------------------------------ | --------------------------------------- | ------------------------------------------- |
| All collected components       | 63.54 / 62.93 / 64.50 / 65.63           | 68.24 / 67.18 / 72.46 / 70.26               |
| `components/` (non-3D)         | 69.59 / 69.88 / 67.54 / 71.91           | 78.88 / 77.47 / 80.65 / 81.16               |
| `components/teaching/`         | 90.50 / 74.58 / 93.75 / 93.52           | 90.96 / 74.89 / 93.85 / 93.79               |
| `components/three/`            | 21.68 / 8.83 / 19.35 / 22.62            | unchanged — not rendered in jsdom by design |
| `McsWorkbench.tsx`             | 77.28 / 79.66 / 78.66 / 80.60           | **95.81 / 93.03 / 98.66 / 98.06**           |
| `McsControls.tsx`              | 60.00 / 89.28 / 51.72 / 58.97           | **100 / 100 / 100 / 100**                   |
| `McsCaseWorkflow.tsx`          | 74.07 / 96.55 / 61.11 / 74.07           | **100 / 100 / 100 / 100**                   |
| `McsLearnPrimaryPane.tsx`      | 90.00 / 100 / 50.00 / 90.00             | **100 / 100 / 100 / 100**                   |
| `McsLearnLanding.tsx`          | 0 / 100 / 0 / 0                         | **100 / 100 / 100 / 100**                   |
| `McsAnatomyPathwaySummary.tsx` | 91.66 / 65.62 / 100 / 91.66             | 100 / 96.87 / 100 / 100                     |
| `McsMonitor.tsx`               | 97.87 / 81.81 / 100 / 100               | 97.87 / 87.27 / 100 / 100                   |
| `McsLearnActionPane.tsx`       | 92.63 / 88.88 / 97.05 / 98.71           | 94.73 / 91.85 / 100 / 100                   |
| `McsHub.tsx`                   | 100 / 90 / 100 / 100                    | 100 / 90 / 100 / 100                        |
| `McsAnatomy3D.tsx`             | 0 / 0 / 0 / 0                           | 0 / 0 / 0 / 0 — by design                   |

The all-files figure is held down by the WebGL components, which the brief excludes from validation.
Excluding `McsAnatomy3D`, `components/three/` and the two lazy previews, the MCS component tree the
M5 brief covers sits at 78.9% statements and 77.5% branches, with every uncovered branch listed above
or owned by an earlier package's suite.

## 7. Deliberate regressions

Fourteen defects were introduced one at a time and reverted; each is recorded in
`docs/critical-care/mcs-m5-regression-log.md` with its exact test, the assertion that failed, and the
confirmation that the file was restored.
