# ECMO-01 test-contract migration

Base: `034648ad120005f00092ef1e56fa896cc3be0c19`. Original tests and the September 13–14 browser failure record remain in Git/history and Local-Data. The owner's self-paced v2 decision changes the intended interaction contract, not the old evidence.

## Changed contracts and preserved checks

Paths below are relative to `src/features/cardiohelp-ecmo/__tests__/` unless stated otherwise. Existing file names are retained for traceability even where “leak”/“Challenge” no longer describes the current product rule.

| Affected tests                                                                                                 | Old assertion                                                                                                                                                | New assertion / unchanged safety or data boundary                                                                                                                                                                                                                                     |
| -------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| New `self-paced.test.tsx`; original-head probe outside Git                                                     | Transfer has a fresh uncommitted bubble scenario and unavailable clamps; paused setup tick has no effect.                                                    | Both tracks enter explicitly, advance the authored interval on the existing STEP path, and close the real return clamp without an answer. Starting changes no prediction/history/credit; bubble latch and pump-stop/resumption interlocks remain. Read-only controls remain disabled. |
| `activity-flow`, `learn-walkthrough`, `b5-vertical-slice-validation`                                           | Commit before protective controls; save lesson completion; old prediction button wording.                                                                    | Protection independent of quiz; explicit transfer Start; visit-only writes. Existing actual control sequences, comparison values, outcome and safety checks retained.                                                                                                                 |
| `learn-precommit-leak.rendered`                                                                                | Traverse to each of 20 predictions and reject answer-bearing DOM, with a known-disclosure exception list.                                                    | All 20 real drill hosts expose the named open outline, optional explanation and skip without committing a response or grading. The obsolete concealment scanner/exception-list metadata is removed. No engine/source tests are removed.                                               |
| `drill-teaching-panels`, `localization-card`                                                                   | Mechanism/response/reflex/localization withheld until commitment and hidden again on reload.                                                                 | Teaching available without a response; only real submitted choices may appear as the learner's choice. Reload clears response evidence. All signal provenance, model-boundary, source and live-model assertions remain.                                                               |
| `circuit-map-emphasis`, `foundation-disclosure-scope`, `drainage-chatter-visibility`, `foundation-answer-leak` | Hide named placements/patterns before a question is answered.                                                                                                | Keep accurate named teaching and implicated segments available. Existing geometry, coordinate identities, sensor validity, absent-marker, no-chatter-when-absent and live-flow checks remain. Shared matcher semantics remain tested.                                                 |
| `stage-sources`                                                                                                | Titles before commitment, supported claims and limits after it.                                                                                              | Titles, claims and limitations remain accessible from entry; source-set identity, counts and correspondence remain tested.                                                                                                                                                            |
| `foundation-activity`, `foundation-phase-restoration`, `foundation-teaching-repair`, `foundation-session`      | Gate later phases, clamp deep links to Predict, persist only transfer completion, use inert outline rows.                                                    | Honest first-task reload, open navigation and direct explanation; visits only. Authored state mapping still governs actual entry/run; real comparisons, clock behavior, clean restoration and preserved actual responses remain tested.                                               |
| `components`, `mode-isolation`, `practice-assess-boundary`, `session-core`                                     | Mask diagnosis/unit names; hold Challenge teaching and hints; ask to discard a quiz attempt; require educational progression; write prediction/grade events. | Meaningful titles, optional hints/reveal, free case/stage navigation, no new graded or help telemetry. Preserve VV/VA filtering, real initiation/control/readiness/observation requirements, immediate safety feedback, debrief domains and actual recorded actions.                  |
| `hub-accordion`, `hub-landing-door-alignment`, `pathway-resolver`                                              | Completion/score fixtures choose next incomplete items.                                                                                                      | Actual self-paced visits and last location choose Continue. Existing history is not inferred as new completion. Hub and Learn use the same current reader/resolver.                                                                                                                   |
| ECMO `routes.test.tsx`                                                                                         | Assess metadata begins Challenge.                                                                                                                            | Metadata begins Integrated cases, with the same noindex and route constraints.                                                                                                                                                                                                        |
| Shared `criticalCareShellConvergence.test.tsx`                                                                 | ECMO fourth label is Challenge.                                                                                                                              | Only ECMO uses Integrated cases; other module expectations and header controls remain.                                                                                                                                                                                                |
| Shared `public-client-boundary.test.ts`                                                                        | Both ECMO and ICU mount restricted graded sync.                                                                                                              | ECMO no longer mounts graded sync; ICU's existing mount and public/restricted catalog graph constraints remain tested.                                                                                                                                                                |

## Retired declarations and explicit replacements

These 16 older declarations represented 19 parameter-expanded assertions. Their titles are recorded individually below. Equivalent useful data/model invariants remain in the noted replacement tests; obsolete exam/inert-row/no-visit-write requirements do not. The actual optional prediction retention behavior is additionally verified by `foundation-phase-restoration`'s new “retains an actual optional prediction when returning through the open task outline” test.

| Original file                           | Original declaration                                                                   | Replacement                                                                                                                                            |
| --------------------------------------- | -------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `foundation-phase-restoration.test.tsx` | opens a predict URL at predict, with the plain restoration note                        | `self-paced`: all foundation sections × six requested phases reopen honestly, open tasks, no restored answers/actions; visit-only persistence.         |
| `foundation-phase-restoration.test.tsx` | clamps a gated-phase URL to predict and says which phase is waiting on the commitment  | `self-paced`: all foundation sections × six requested phases reopen honestly, open tasks, no restored answers/actions; visit-only persistence.         |
| `foundation-phase-restoration.test.tsx` | opens the VA baseline at %s with no snapshot                                           | `self-paced`: all foundation sections × six requested phases reopen honestly, open tasks, no restored answers/actions; visit-only persistence.         |
| `foundation-phase-restoration.test.tsx` | does not reload any state when a performed step row is reviewed                        | Open task navigation executes authored entry states without fabricating performed steps; existing state-mapping and interaction-evidence tests remain. |
| `foundation-phase-restoration.test.tsx` | keeps a committed prediction across a step change                                      | `foundation-phase-restoration`: real optional answer retained on return; retry remains available.                                                      |
| `foundation-phase-restoration.test.tsx` | canonicalizes to VA before clamping when VV is asked for                               | `self-paced`: all foundation sections × six requested phases reopen honestly, open tasks, no restored answers/actions; visit-only persistence.         |
| `foundation-phase-restoration.test.tsx` | canonicalizes to VV before clamping when VA is asked for                               | `self-paced`: all foundation sections × six requested phases reopen honestly, open tasks, no restored answers/actions; visit-only persistence.         |
| `foundation-phase-restoration.test.tsx` | touches no storage while opening at a mapped phase                                     | `self-paced` + `foundation-teaching-repair`: genuine topic visit only, unchanged legacy values, no graded or completion writes.                        |
| `foundation-phase-restoration.test.tsx` | carries the phase in the URL with replaceState, never pushState, and in nothing stored | `foundation-activity`: phase via replaceState, no extra browser history; `self-paced`: saved location only, no responses or actions restored.          |
| `foundation-phase-restoration.test.tsx` | carries the phase in the component key and in the URL, and nowhere else                | `foundation-activity`: phase via replaceState, no extra browser history; `self-paced`: saved location only, no responses or actions restored.          |
| `foundation-phase-restoration.test.tsx` | writes nothing on mount and exactly one traversal marker on transfer commit            | `self-paced` + `foundation-teaching-repair`: genuine topic visit only, unchanged legacy values, no graded or completion writes.                        |
| `foundation-phase-restoration.test.tsx` | does not write again when a section that was already worked is committed again         | `self-paced` + `foundation-teaching-repair`: genuine topic visit only, unchanged legacy values, no graded or completion writes.                        |
| `foundation-phase-restoration.test.tsx` | leaves row review inert, so only the Back control moves the learner                    | Open task navigation executes authored entry states without fabricating performed steps; existing state-mapping and interaction-evidence tests remain. |
| `foundation-answer-leak.test.tsx`       | %s: the predict phase, with the stem on screen, still discloses nothing                | Rendered full-label and map-emphasis tests; direct explanation with no fabricated response.                                                            |
| `foundation-answer-leak.test.tsx`       | %s: the diagnostic map withholds the channel placements, in every layer                | `self-paced`: all foundation sections × six requested phases reopen honestly, open tasks, no restored answers/actions; visit-only persistence.         |
| `foundation-answer-leak.test.tsx`       | selecting the Pressure-zone map tab before committing exposes nothing                  | Rendered full-label and map-emphasis tests; direct explanation with no fabricated response.                                                            |

## Evidence accounting

The original ECMO baseline discovered and passed 69 suites / 2,099 tests. Intermediate red suites are retained (`working*.log` and JSON). Those failures include deliberately obsolete exam contracts and genuine test/implementation corrections; none are counted as final passing evidence. The original-head probe separately reproduces both blocked transfer surfaces. Final counts and commands live in [the handoff](ECMO-01.md), with exactly selected consumer tests distinguished from entire suites. Existing historical progress/scoring unit tests remain as compatibility tests; passing them does not enable grading in current ECMO sessions.
