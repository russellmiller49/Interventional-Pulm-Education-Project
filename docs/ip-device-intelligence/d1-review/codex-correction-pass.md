# Phase D1 review packet — independent Codex review findings and corrections

Correction-pass record (2026-08-09). An independent Codex review of the frozen RC pair
`978279f2 → 916f3fcc` returned eight confirmed findings (two HIGH, four MEDIUM, two LOW).
This session merged current `origin/main` (`ee3b33e9`) once, corrected all eight at the
runtime level, and added regressions exercising the live integration paths. PR #88 remains
draft and unmerged; targeted verification ranges are recorded in the PR body.

| #    | Severity | Finding                                                                                                                                                                                                                                                                                      | Disposition                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ---- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C-01 | HIGH     | A required role with structural no-coverage (`proposals_only` or the two unauthored rungs) read `ready_with_limitations` when a demo stand-in supplied a selection — live for GENERIC_SUCTION in EBUS_TBNA and THERAPEUTIC_BRONCH, whose headlines showed amber.                             | **Fixed** in `domain/readiness.ts`: a required requirement whose coverage rung is in `NO_COVERAGE_RUNGS` is `not_ready` regardless of any selection; the `missing_required_product_role` diagnostic, resolver advisories, and the disclosed demo mapping (`evidence`) are unchanged. EBUS_TBNA and THERAPEUTIC_BRONCH headlines are now **Demo: Not ready**. Regressions: unit (all three rungs, with stand-in selection; conditional counter-case), live GENERIC_SUCTION paths for both procedures, rendered-headline pin.                                                                                                                                                                                                                                                                                                                                 |
| C-02 | HIGH     | Procedure-workspace authored options resolved identities from the full catalog; `atlasVisible=false` suppressed only the link, so 274 unique non-cohort product identities (e.g. PRD-F43B951B75 "Micro Retrieval Net" on SLOT-115310F554) reached the public view model and rendered output. | **Fixed** at the server view-model boundary (`procedures.server.ts`): option identities are read from the atlas store only; non-cohort authored options surface solely as `withheldAuthoredOptionCount` / `withheldSelectableOptionCount` on the requirement (fail-closed shape — no hidden id rides inside a redacted object). The browser renders an honest withheld line and never claims a slot is option-free when identities are merely withheld. Regressions: data-wide over all three exemplars (cohort-membership of every exposed id; withheld counts re-derived independently; no non-cohort id serializes), the Codex exemplar pinned exactly (3 exposed / 4 withheld / 0 withheld-selectable; name and id absent from model and rendered page; direct atlas route still 404).                                                                  |
| C-03 | MEDIUM   | A raw compatibility statement with one resolved cohort participant and one textual participant could print an exact hidden-product identifier — live for RULE-04C5B71790 on PRD-6FF6668D03, whose text names BF-MP190F (= hidden PRD-CB1622624D's catalog number).                           | **Fixed** in `compatibility.server.ts`: before any statement text reaches a public atlas surface, participant fields and rule text are checked against the catalog's exact normalized identifiers (`searchableIds` + product id; whole-field and exact-token lookups, no fuzziness; short tokens excluded from free-text scanning as non-deterministic). Deterministic non-cohort references return a `WithheldCompatibilityStatement` (provenance only) and pages render a generic locale-backed explanation. The product record's own `compatibility_text` passes the same wall (`compatibilityTextWithheld`). Ambiguous identifiers fail closed. Regressions: all four GuideSheath rules withheld with BF-MP190F/PRD-CB1622624D absent from serialized output; cohort-safe statements still render; data-wide token sweep over every cohort device page. |
| C-04 | MEDIUM   | Formulary eligibility used a procedure-wide `authoredSelectableForProcedure` boolean, so a row mapping a product to role B was satisfied by the product's selectability for unrelated role A, suppressing `inventory_formulary_mismatch`.                                                    | **Fixed**: `FormularyAssertionInput` now carries the row's parsed `roleCodes`, and the projection judges eligibility against `authoredSelectableProductIdsByRole` — built from THIS procedure's own slots per role. The procedure-wide boolean no longer exists, so it cannot be bypassed. Hidden-product rows stay mismatches. Regressions: unit (role-B mismatch / role-A match / hidden fail-closed) plus a server-integration suite that mocks the formulary artifact and drives `getProcedureReadinessView` end-to-end.                                                                                                                                                                                                                                                                                                                                |
| C-05 | MEDIUM   | Governed selection-guidance strings ("Hospital formulary item.", "Match local formulary…") rendered on public role pages with no adjacent statement that they are generic authored content, not a real institution's formulary.                                                              | **Fixed**: a single global `deviceIntelligence.role.guidanceGenericNote` qualifier renders adjacent to the guidance block in BOTH branches (prose and criteria) of every clinical-role page — no role-code special-casing; governed strings stay verbatim; keys added to en/es/zh-CN (parity-tested). Regressions: rendered qualifier beside the verbatim "Hospital formulary item." on GENERIC_DRAINAGE_UNIT and on a non-formulary role (global proof).                                                                                                                                                                                                                                                                                                                                                                                                   |
| C-06 | MEDIUM   | The F-18 Primary-fit representative preference was implemented inside shared `getProductDetail()` default behavior, changing related-product representative ids on 114 preserved preference-card product pages, contrary to the D1 preservation contract.                                    | **Fixed**: `getProductDetail` gained `options.representativeSelection` — default `'catalog_order'` reproduces the exact pre-D1 representative ids; `'primary_fit'` is passed ONLY by `getAtlasProductDetail`. The store-keyed Fuse-cache fix and both feature-gated cross-links are untouched. Regressions: synthetic within-group divergence (default vs opt-in), data-wide legacy-id pin across the whole real catalog, atlas-path opt-in equivalence, and a proof the two modes genuinely differ on real data. `owner-review-dispositions.md` F-18 carries the supersession note.                                                                                                                                                                                                                                                                        |
| C-07 | LOW      | The laser safety disclosure rendered from `workspace.procedureCode === 'THERAPEUTIC_BRONCH'`.                                                                                                                                                                                                | **Fixed**: new pure `domain/laser-pathway.ts` derives `laserPathwayDisclosureRequired` (≥1 `LASER*` governed requirement AND none with an authored selectable option, computed from raw pre-withholding option rows); the workspace view model carries the fact and the page renders from it. Truth-pinned present (THERAPEUTIC_BRONCH, incl. rendered) and absent (EBUS_TBNA, CHEST_TUBE, incl. rendered), plus helper unit rules (including the deliberate PHOTODYNAMIC_LASER exclusion).                                                                                                                                                                                                                                                                                                                                                                 |
| C-08 | LOW      | Review artifacts carried a stale 76-test count; the browser walkthrough presented pre-owner-correction observations (IFU banner, Nursing grouping, amber readiness headlines, bare Ready chips) as current; copy called preserved direct-link catalog surfaces "authenticated".              | **Fixed**: counts synchronized (11 suites / 106 focused tests); `browser-walkthrough.md` carries a historical-evidence banner enumerating superseded observations with links to the owner-correction and validation records (referrers caveated); spec-vs-built deltas annotated in `vertical-slice-spec.md`; "authenticated" replaced with public-unlisted/direct-link in the two locale strings (×3 locales), two source doc-comments, and `d1-implementation.md` — while the owner's verbatim historical findings remain untouched. PR body updated after final gates.                                                                                                                                                                                                                                                                                   |

## Boundaries honored

No governed/seed/generated data changed; the D0 audit artifact is byte-identical
(`npm run ip-intel:audit`); no migration, release bundle, pointer, or family approval moved;
no flag, navigation, sitemap, or indexing change; no persistence/API/action/write path added;
proposals still never satisfy coverage, readiness, or outputs; no equivalence or substitution
inference introduced. PR #88 remains draft.

## Residual finding C-04b — multi-role row all-relevant-role eligibility (2026-08-09)

Targeted Codex verification of the correction head `4f6c5695` **passed C-01, C-02, C-03,
C-05, C-06, C-07, C-08, the synthetic merge with current main, and all gates**, and returned
one reproducible MEDIUM residual inside C-04:

- **Reproduction** (through `getProcedureReadinessView('EBUS_TBNA')`, product
  `PRD-2302DA77DA`, all rows `hospital_carries=true`): rows `FORM-B-ONLY`
  (GENERIC_SUCTION), `FORM-A-ONLY` (EBUS_NEEDLE_FNA), `FORM-A-AND-B`
  (GENERIC_SUCTION; EBUS_NEEDLE_FNA) produced `mismatches = ["FORM-B-ONLY"]` — the
  multi-role row's matching EBUS_NEEDLE_FNA suppressed its GENERIC_SUCTION mismatch. The
  correct result is `["FORM-A-AND-B", "FORM-B-ONLY"]`.
- **Why the prior regression missed it**: the C-04 fix replaced the procedure-wide boolean
  with per-role eligibility but combined the row's roles with `.some(...)` — "eligible for
  at least one named role" — and every C-04 regression used single-role rows, so the
  any-vs-all distinction was never exercised.
- **Corrected semantics** (`domain/readiness.ts`): the server assembly intersects the row's
  parsed role codes with the procedure's own role set (deduplicated, sorted) and passes only
  those procedure-relevant roles into `FormularyAssertionInput.roleCodes`; the projection
  derives `relevantRoleCodes` / `mismatchingRoleCodes` / `eligibleForAllRelevantRoles`, and
  a carried or preferred row mismatches when `eligibleForAllRelevantRoles` is false or the
  product is hidden. Every relevant role must independently authorize the product; one
  matching role never suppresses another's mismatch; an empty relevant set fails closed
  (never `every([])` vacuous truth) — and the naive "every over the RAW parsed codes"
  variant is equally ruled out, because a role the row names for a different procedure is
  removed by the intersection instead of failing the row. The single row-level diagnostic
  (`sourceKind: formulary_row`, `sourceId: formularyId`) reports the product id, carried and
  preferred states, the sorted relevant-role list, the sorted mismatching-role list, the
  all-relevant-roles eligibility result, and the visibility state.
- **Regressions added**: unit (A-only / B-only / A+B partial with the diagnostic naming
  GENERIC_SUCTION's analogue / A+B full match / empty-relevant-set fail-closed /
  hidden-with-all-roles-passing / duplicate-and-shuffled input determinism) and the exact
  A+B server-integration reproduction in `readiness-formulary.test.ts` (pre-import mock →
  `getProcedureReadinessView('EBUS_TBNA')`; `FORM-A-AND-B` mismatches with
  `mismatchingRoles=GENERIC_SUCTION`, `FORM-A-ONLY` does not, exact mismatch set pinned)
  plus a multi-role hidden-product row and a real-but-non-EBUS role (`TALC_VIAL`) row pair
  proving the intersection drops unrelated roles without creating false mismatches and that
  the unrelated role never survives into the domain assertion.

No other C-01..C-08 behavior was modified in the C-04b pass.
