import type { MDXComponents } from 'mdx/types'

import { Callout, CodeBlock, DownloadButton, ImageComparison, Quiz, VideoEmbed } from './index'

export const mdxComponents: MDXComponents = {
  Callout,
  CodeBlock,
  ImageComparison,
  VideoEmbed,
  DownloadButton,
  Quiz,
}
